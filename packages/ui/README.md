# Shared UI package

Reserved for Tailwind design tokens and shared shadcn/ui primitives. It must contain presentational components only, without database, authentication, or domain-service dependencies.
