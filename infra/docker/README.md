# Docker infrastructure

`docker-compose.yml` starts PostgreSQL with pgvector, Redis, and MinIO for local development. Add service Dockerfiles and production deployment definitions only alongside corresponding implementation and security hardening.
